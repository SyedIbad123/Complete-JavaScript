 //  The Map object holds key-value pairs and remembers the original insertion order of the keys.
 //  In Map, key is of any type but in objects, it is only string or symbol.


 //  Example:

 const obj = new Map();
 console.log(obj);   //  Map(0) {}