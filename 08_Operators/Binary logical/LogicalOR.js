 //  This operator for a set of operands is true if only if one or more of its operands is true. It will return a non-boolean value if it is used with non-boolean values.

 //  Example:

 const a = 3;
 const b = -2;
 console.log(a>0 || b>0);  // true