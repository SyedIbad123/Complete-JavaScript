 // The Null data type or value represents intentional absence of any object vlue. It is one of JS's primitve value and is treated as falsy for boolean expressions.

 //  Example:
 
 const foo = null; // foo is known to exist but it has no type or null type or no value.
 console.log(foo);

 // Output:
//  null