 //  The while statement creates a loo that executes a specified statement as lon as the test condition is evaluates to true. The condition is evaluated before executing the statement.

 //  Example:

 let n = 0;
 let x = 0;
 while(n<3){
    n++
    x+=n;
 }
 console.log(x);

 // Output:
 //  6

